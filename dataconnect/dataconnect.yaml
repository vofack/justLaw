specVersion: "v1beta"
serviceId: "justLaw"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "justLaw-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
